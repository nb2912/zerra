version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run docs:build
  artifacts:
    baseDirectory: packages/docs/out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - packages/docs/.next/cache/**/*
