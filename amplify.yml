version: 1
applications:
  - appRoot: packages/docs
    frontend:
      phases:
        preBuild:
          commands:
            - rm -rf .next
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: out
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
